/*
Write a JavaScript code in a Web page dec2hex.html that enters
 a positive integer number num and converts and converts it to 
 a hexadecimal form. The input should be entered as JavaScript
 prompt window. The output should be shown as browser popup 
 window (alert). Examples:
*/

function decTohex()
{
	var dec = parseInt(prompt("Please enter decimal number"));
	if(dec != null){
		alert(dec.toString(16).toUpperCase());
	}
	
}
